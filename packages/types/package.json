{
  "name": "@root/types",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "prepare": "yarn run build",
    "prebuild": "yarn run clean",
    "clean": "tsc -b --clean"
  },
  "engines": {
    "node": "12.x",
    "yarn": "1.x"
  },
  "devDependencies": {
    "typescript": "^4.2.3"
  }
}