{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "lerna run build --stream",
    "start": "lerna run start --stream"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
